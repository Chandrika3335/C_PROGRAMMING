#include<stdio.h>
int main()
{
	int num,digit,sum=0;
        printf("enter the number\n");
        scanf("%d",&num);
test : 
	
		digit = num % 10;
		sum = sum + digit;
		num = num / 10;
	if(num>0)
	{
		goto test;
	}
	printf("Sum of all digits is %d\n",sum);
}

